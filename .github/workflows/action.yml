name: 'GitHub Actions Weather Bot'

on:
  push:
  schedule:
    - cron: '0 21 * * *'

jobs:
  bot:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout codes'
        uses: actions/checkout@v1
      - name: 'Get Result'
        run: bash ./weather.sh
      - name: 'Send mail'
        uses: dawidd6/action-send-mail@master
        with:
          server_address: smtp.live.com
          server_port: 465
          username: se7enxin@outllook.com
          password: aolong123654
          subject: Beijing Weather Report
          body: file://result.html
          to: xin.zhang@key-power.com.cn
          from: GitHub Actions
          content_type: text/html
